package network.mememc.memeac.checks.combat;

import network.mememc.memeac.MemeAC;
import network.mememc.memeac.checks.Check;

public class AutoClickerCheck extends Check {
    public AutoClickerCheck(MemeAC plugin) { super(plugin, "AutoClicker", CheckType.COMBAT, 7, 99.6); }
}
