package network.mememc.memeac.checks.combat;

import network.mememc.memeac.MemeAC;
import network.mememc.memeac.checks.Check;

public class AimAssistCheck extends Check {
    public AimAssistCheck(MemeAC plugin) { super(plugin, "AimAssist", CheckType.COMBAT, 9, 97.5); }
}
